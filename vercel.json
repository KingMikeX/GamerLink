{
    "builds": [
      {
        "src": "webapp/package.json",
        "use": "@vercel/next",
        "config": {
          "installCommand": "npm install"
        }
      }
    ],
    "routes": [
      {
        "src": "/(.*)",
        "dest": "/webapp/$1"
      }
    ]  
  }