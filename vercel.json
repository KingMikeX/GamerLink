{
  "builds": [
    {
      "src": "webapp/package.json",
      "use": "@vercel/next",
      "config": {
        "outputDirectory": "webapp"
      }
    }
  ],
  "installCommand": "npm install --prefix webapp"
}