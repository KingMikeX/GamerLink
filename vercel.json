{
    "builds": [
      {
        "src": "Backend/src/main.py",
        "use": "@vercel/python",
        "config": {
          "installCommand": "python3 -m pip install -r Backend/src/requirements.txt"
        }
      },
      {
        "src": "webapp/package.json",
        "use": "@vercel/next",
        "config": {
          "installCommand": "npm install"
        }
      }
    ],
    "routes": [
      {
        "src": "/api/(.*)",
        "dest": "Backend/src/main.py"
      },
      {
        "src": "/(.*)",
        "dest": "webapp/$1"
      }
    ]  
  }